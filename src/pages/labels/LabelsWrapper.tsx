import styled from 'styled-components';

const LabelsWrapper = styled.div`
  .page--labels {
    padding: 20px 0;

    .labels-table {
      margin: 20px 0 0;
    }
  }
`;

export default LabelsWrapper;
